<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    android:label="Geofences"
    app:startDestination="@id/geofence">

    <fragment
        android:id="@+id/geofence"
        android:name="com.location.geofencesapp.GeofenceHome"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_geofence" />
    <fragment
        android:id="@+id/fence_list_frag"
        android:name="com.location.geofencesapp.fence_list"
        android:label="Geofences"
        tools:layout="@layout/fragment_fence_list"></fragment>
</navigation>